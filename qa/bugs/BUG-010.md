ID: BUG-010 | Area: Mockup | Severity: P1
Route/File: src/hooks/mockup/mockupGenerationService.ts
Steps to reproduce: Generate mockup with multiple areas
Result: TypeScript error — `singleUrl` and `batchResults` not on GenerateMockupResult
Expected: Result includes singleUrl and batchResults for batch generation
Root cause: Interface GenerateMockupResult missing batch fields (singleUrl, batchResults)
Fix: Added `singleUrl?: string | null` and `batchResults: { url: string; areaName: string }[]` to interface
Test: TypeScript compiler
