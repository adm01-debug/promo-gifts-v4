# OBSOLETO (2026-07-12).
#
# Este arquivo .patch foi descontinuado após auditoria exaustiva descobrir que
# GNU `patch(1)` rejeitava seus cabeçalhos de hunk mesmo quando o conteúdo era
# byte-idêntico ao arquivo alvo (comportamento reproduzido em ~40 simulações).
#
# A migração agora é feita 100% via sed/awk determinístico em
# `docs/plans/apply-magazine-typed-queries.sh`, com verificações estritas
# (contagem antes/depois, balanceamento sintático, aliases presentes,
# import contíguo, ausência de interface órfã e de `void supabase;`).
#
# Se você chegou aqui esperando aplicar este .patch: NÃO. Rode:
#   bash docs/plans/apply-magazine-typed-queries.sh
